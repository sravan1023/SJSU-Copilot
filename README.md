# academic-copilot
A full-stack chatbot application that retrieves and answers queries about professor office hours using data from Excel. The system uses a React-based chat interface and a Python backend to process structured and semi-structured data, enabling students to easily find faculty availability and book appointments.
